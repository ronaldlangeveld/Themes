routes:

collections:
  /:
    permalink: /{slug}/
    template: home
    filter: 'tag:gallery'
  /posts/:
    permalink: /{slug}/
    template: posts
    data: page.posts

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/