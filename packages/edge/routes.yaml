routes:

collections:
  /:
    permalink: /{slug}/
    template: index
    filter: 'tag:gallery'
  /posts/:
    permalink: /{slug}/
    template: posts

taxonomies:
  tag: /tag/{slug}/
  author: /author/{slug}/